fn main() {
    println!("Hello, World from Rust!!!");
    println!("println without ! will cause error, because ! implicate that println is macro");
    println!("name of the fn is case sensitive so if I rename main to Main it will cause error")
}
